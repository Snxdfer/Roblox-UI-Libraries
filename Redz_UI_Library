loadstring(game:HttpGet("https://raw.githubusercontent.com/Snxdfer/back-ups-for-libs/refs/heads/main/RedzUI.lua"))()

MakeWindow({
  Hub = {
    Title = "REDz HUB",
    Animation = "by : redz9999"
  },
  Key = {
    KeySystem = false,
    Title = "Key System",
    Description = "",
    KeyLink = "",
    Keys = {"1234"},
    Notifi = {
      Notifications = true,
      CorrectKey = "Running the Script...",
      Incorrectkey = "The key is incorrect",
      CopyKeyLink = "Copied to Clipboard"
    }
  }
})

MinimizeButton({
  Image = "",
  Size = {40, 40},
  Color = Color3.fromRGB(10, 10, 10),
  Corner = true,
  Stroke = false,
  StrokeColor = Color3.fromRGB(255, 0, 0)
})

local Main = MakeTab({Name = "Main"})

MakeNotifi({
  Title = "REDz HUB",
  Text = "Notification test",
  Time = 5
})

local section = AddSection(Main, {"Test"})


AddButton(Main, {
  Name = "Button test",
  Callback = function()
    
  end
})

local Toggle = AddToggle(Main, {
  Name = "Toggle test",
  Default = false,
  Callback = function(Value)
    
  end
})

local Slider = AddSlider(Main, {
  Name = "Slider test",
  MinValue = 10,
  MaxValue = 100,
  Default = 25,
  Increase = 1,
  Callback = function(Value)
    
  end
})

AddKeybind(Main, {
  Name = "Keybind test",
  KeyCode = "E",
  Default = false,
  Callback = function(Value)
    
  end
})

AddTextBox(Main, {
  Name = "TextBox test",
  Default = "good day",
  PlaceholderText = "test",
  ClearText = true,
  Callback = function(Value)
    
  end
})

local Dropdown = AddDropdown(Main, {
  Name = "Dropdown test",
  Options = {"1", "2", "3"},
  Default = "2",
  Callback = function(Value)
    
  end
})

AddColorPicker(Main, {
  Name = "Color picker test",
  Default = Color3.fromRGB(255, 255, 0),
  Callback = function(Value)
    
  end
})

local Label = AddTextLabel(Main, "Fixed LOL")
SetLabel(Label, "ez fix")

local Paragraph = AddParagraph(Main, {"Note", "Fix"})
SetParagraph(Paragraph, {"Note", "LOL!"})

local Image = AddImageLabel(Main, {
  Name = "Cool Image",
  Image = "rbxassetid://"
})

local MobileToggle = AddMobileToggle({
  Name = "Toggle",
  Visible = true,
  Callback = function(Value)
    
  end
})
